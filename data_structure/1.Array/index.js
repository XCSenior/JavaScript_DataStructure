// 声明
const arr = new Array(1, 2, 3);
// 字面量声明
const arr2 = [1, 2, 3];

// 根据索引值来进行元素的访问
arr[0];

/* 1、遍历所有元素：for循环 */
for (let i = 0; i < arr.length; i++) {
    const element = arr[i];
}

/* 2、数组方法：pop、push、shift、unshift */









